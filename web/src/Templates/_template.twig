<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>Gerencia de extintor</title>

  <link rel="stylesheet" href="{{ rootURL }}/styles/global.css" />
  <link rel="stylesheet" href="{{ rootURL }}/styles/header.css" />
  <link rel="stylesheet" href="{{ rootURL }}/styles/footer.css" />

  {% block styles %}
  {% endblock %}
</head>

<body>
  <header>
    <nav>
      <a href={{ url_for("home") }}>Home</a>
      <a href={{ url_for("list-extinguishers") }}>Extintores</a>
      <a href={{ url_for("list-locations") }}>Locais</a>
    </nav>
  </header>

  {% block content %}
  {% endblock %}

  <footer>
    <span>
      &copy; Gerencia de extintores
    </span>
  </footer>

  <script src="{{ rootURL }}/js/global.js"></script>

  {% block scripts %}
  {% endblock %}
</body>

</html>
